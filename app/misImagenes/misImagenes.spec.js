'use strict';

describe('myApp.misImagenes module', function() {

  beforeEach(module('myApp.misImagenes'));

  describe('misImagenes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var misImagenesController = $controller('MisImagenesController');
      expect(misImagenesController).toBeDefined();
    }));

  });
});