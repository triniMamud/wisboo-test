'use strict';

describe('myApp.buscarImagenes module', function() {

  beforeEach(module('myApp.buscarImagenes'));

  describe('buscarImagenes controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var buscarImagenesController = $controller('BuscarImagenesController');
      expect(buscarImagenesController).toBeDefined();
    }));

  });
});